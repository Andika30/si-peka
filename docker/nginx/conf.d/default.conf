server {
    listen 80;
    server_name peka.meambo.id;

    # Formulir masuk admin gak boleh bisa di-iframe dari domain lain
    # (clickjacking), dan peramban jangan menebak-nebak tipe konten.
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # dukungan HMR/websocket kalau container ini juga dipakai buat dev
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
